main {
    # Floating can only be cast to: Integer, Floating
    var v := 12.34;

    var a := <bool>(v);
    var b := <char>(v);
    var c := <string>(v);
    var d := <int>(v);
    var e := <float>(v);
}