
subr int syrup (int number) {
	var habanero := 0.25;
	if(number / 2 == 0) {
		habanero := number / 2;
	} 
	else {
		habanero := 3 * number + 1;
	}
	return habanero;
}


main {
	const min := 90;
	const max := 100;
	
	var count := min;
	while (count < max) {
		count := count + 1;
	}
	print "done." \n;
}
