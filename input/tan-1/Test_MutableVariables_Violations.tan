# uncomment a specific section to test.
# each test case below should throw error(s) as expected.

main {
    # 1. Declaring the same variable again should throw error:
#    var i:= 10;
#    var i:= 11;

    # Expected error: variable "i" multiply defined at (mutableVariablesTest_Violations.tan line 7, character 8)

    #==================================================================================================================

    # 2. Assigning a variable with a value of a different type:
#    var i := 1.2;
#    i := 1;

    # Expected error: types do not match in AssignmentStatement

    #==================================================================================================================

    # 3. Assigning a variable with a another variable of a different type:
#    var i := 100;
#    const j := 100.11;
#    i := j;

    # Expected error: types do not match in AssignmentStatement

    #==================================================================================================================

    # 4. Re-assigning value to a variable that was not declared before:
#    var i := 1;
#    i := 22;
#    a := 100;

    # Expected error: identifier "a" used before defined at (mutableVariablesTest_Violations.tan line 33, character 4)

    #==================================================================================================================

    # 5. Re-assigning value to a const variable:
    const i := 1;
    i := 22;

    # Expected error: re-assignment to a const identifier, previously declared at location: (Test_MutableVariables_Violations.tan line 40, character 10)

}